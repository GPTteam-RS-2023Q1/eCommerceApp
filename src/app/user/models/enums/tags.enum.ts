export enum Tag {
  defaultShippingAddress = 'default shipping address',
  defaultBillingAddress = 'default billing address',
  shippingAddress = 'shipping address',
  billindAddress = 'billing address',
}
