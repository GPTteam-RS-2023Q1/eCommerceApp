import { GetSizeOfVariantPipe } from './get-size-of-variant.pipe';

describe('GetSizeOfVariantPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSizeOfVariantPipe();
    expect(pipe).toBeTruthy();
  });
});
