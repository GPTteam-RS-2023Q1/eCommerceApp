import { TranslateAttributePipe } from './translate-attribute.pipe';

describe('TranslateAttributePipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateAttributePipe();
    expect(pipe).toBeTruthy();
  });
});
